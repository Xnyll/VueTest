To run :
Fork the repo, navigate into the vue-project folder and npm install to get the dependencies (Node v20.11.0) 
npm run dev to run the vue project on your localhost which should be configured, npx playwright test to run the e2e tests

Improvements :
Styling work, icon fixes, additional functionality e.g editing/deleting tasks and potential refactoring of code & e2e script
